# Python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store

# Regenerable artifacts (scripts/01 rebuilds in <1s)
outputs/elo_history.parquet

# Raw HTML caches (refetchable; parsed CSVs are committed)
data/external/*.html
data/external/tmax_*.json

# footballdatabase raw caches (consolidated into fdb_history.json)
data/external/fdb_hist/*.json
data/external/fdb_rank/*.html
data/external/fdb_country/*.html

# API keys / secrets — never commit
.secrets/
